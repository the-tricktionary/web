query SpeedResults ($limit: Int, $startAfter: Timestamp, $eventDefinitionId: ID, $groupId: ID) {
  me {
    id
    speedResults (limit: $limit, startAfter: $startAfter, eventDefinitionId: $eventDefinitionId, groupId: $groupId) {
      ...SpeedResultBase
    }
  }
}
