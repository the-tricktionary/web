mutation SetGroupMemberTrickCompletion ($memberId: ID!, $trickId: ID!, $completed: Boolean!) {
  setGroupMemberTrickCompletion (memberId: $memberId, trickId: $trickId, completed: $completed) {
    id
    trick {
      id
    }
    recordedBy {
      id
      name
    }
  }
}
