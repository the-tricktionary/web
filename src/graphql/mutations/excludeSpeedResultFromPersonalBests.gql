mutation ExcludeSpeedResultFromPersonalBests ($speedResultId: ID!, $excluded: Boolean!) {
  excludeSpeedResultFromPersonalBests (speedResultId: $speedResultId, excluded: $excluded) {
    id
    excludedFromPersonalBests
  }
}
